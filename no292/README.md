## 芸名
t文字目とu文字目目を削除する

## results
@ruby

## notes
* 文字列を配列で受け取る
* 指定の場所の文字を削除する(rubyはdelete_atというのがあるらしい)
* joinして表示
* 指定の場所を同時に削除できないので、要素を削除した場合、tとuの大小で2回目の削除位置の調整が必要
 * [t, u].minのkeyをdeleteすれば先に小さい要素を削除
 * [t, u].max-1のkeyをdeleteすれば、次に大きい要素の位置を調整して削除できる